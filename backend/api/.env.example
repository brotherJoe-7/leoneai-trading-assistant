# ── LeoneAI Backend — Environment Variables ──────────────────────────
# Copy this file to .env and fill in your values

# ── Database ──────────────────────────────────────────────────────────
# Option A: SQLite (default, for quick local dev — no setup needed)
DATABASE_URL=sqlite:///./leoneai.db

# Option B: PostgreSQL (recommended for production with pgAdmin)
# Format: postgresql://username:password@host:port/database_name
# Example: DATABASE_URL=postgresql://leoneai_user:MySecurePass123@localhost:5432/leoneai_db
#
# pgAdmin setup steps:
#   1. Open pgAdmin → right-click Databases → Create → Database
#   2. Name it: leoneai_db
#   3. Create a user: right-click Login/Group Roles → Create role
#   4. Grant the role access to leoneai_db
#   5. Paste the connection string above (replacing username/password)
#
# DATABASE_URL=postgresql://leoneai_user:MySecurePass123@localhost:5432/leoneai_db

# ── Security ──────────────────────────────────────────────────────────
# Generate a strong secret: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=change-me-to-a-long-random-string-before-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ── Environment ───────────────────────────────────────────────────────
ENVIRONMENT=development
API_HOST=0.0.0.0
API_PORT=8000

# ── CORS (comma-separated origins) ────────────────────────────────────
# Add your Vercel URL here after deployment:
# ALLOWED_ORIGINS=http://localhost:5173,https://leoneai.vercel.app

# ── Payments (optional, add when integrating live payments) ───────────
# STRIPE_SECRET_KEY=sk_test_...
# PAYPAL_CLIENT_ID=...
# PAYPAL_SECRET=...